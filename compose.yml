services:
  bot:
    build: .
    container_name: logbook-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    stop_grace_period: 10s
